# data/ への直接アクセスを禁止する(配信は必ず api.php 経由 = トークン検証を通す)。
# Apache 2.4
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
# Apache 2.2
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
