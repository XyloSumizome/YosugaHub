# 追加設計指示: AI役割分離 — ヨスガ / レコル(v4.3)

> **状態: 指示を受領・運用変更が主(2026-07-23)**。
> 本書は v4(`yosuga_hub_android_design_v4.md`)への追加仕様。
> ユーザー文書内の表記は「Yosuga Hub v2 仕様変更」だが、リポジトリの設計書番号としては
> v4.3 として管理する(設計書v2=技術資料との混同を避けるため)。
> アプリ実装の必須変更は**なし**(付録「現状実装との対応」参照)。

---

## 指示本文(ユーザーより)

# Yosuga Hub v2 仕様変更

## 概要

Yosuga Hubの運用方針を変更します。

AIの役割を2つに分離します。

---

## 役割

### ヨスガ

担当

- シロとの会話
- ゲーム制作の相談
- 設計
- アイデア出し
- プログラミング相談
- 世界観構築
- 長時間の思考支援

特徴

- Hubを直接更新しない
- 会話の相棒
- セッション中の文脈を保持する

---

### レコル

担当

- Hub管理
- タスク整理
- 文書分類
- diary生成
- projectHealth分析
- directives更新
- conversations整理
- index管理

特徴

- ActionからHubへアクセスする
- Hubの唯一の管理者
- すべてのJSON更新を担当

---

## 運用フロー

朝

レコル

↓

Hubを読み込み

↓

Morning Brief生成

↓

シロがヨスガへ渡す

↓

ヨスガが内容を読み、その日の制作を開始する

---

制作中

ヨスガ

↓

思考支援

↓

必要ならレコルへ依頼

↓

レコルがHub更新

---

終了時

レコル

- diary更新
- conversations更新
- tasks更新
- projectHealth更新
- 必要に応じてdocuments分類

---

## Morning Brief

新規概念

Morning Brief

目的

ヨスガがその日の制作に必要な情報だけを取得するための要約。

推奨構成

# Morning Brief

## 昨日の成果

## 今日の予定

## 未完了タスク

## 最新の決定事項

## 現在の課題

## 注意事項

## シロの状態
(疲労・モチベーション等)

この情報量は1~5KB程度を推奨する。

---

## 会話ログ

conversation.jsonは保存を継続する。

Morning Brief生成時には

最新会話から重要事項のみ抽出して利用する。

全文は読み込まない。

---

## 設計思想

Hubは長期記憶。

ヨスガは短期思考。

レコルは長期記憶の管理者。

ヨスガは必要最小限の情報のみ受け取り、
セッション中はその文脈を保持して制作を支援する。

これにより

・大量ログを毎回読み込まない
・会話品質を維持する
・Hubを唯一の正とする
・AIが変わっても記憶を継承できる

という構成を目指す。

---
---

# 付録: 現状実装との対応(Claude Code による分析 / 2026-07-23)

## この分離が生まれた経緯

2026-07-23 の実地検証で、**データを読めるAIと会話を持つAIが別になる**ことが判明した:

- カスタムGPT(Actions): api.php を読める ✅ / 通常会話・ボイスのログを共有しない ❌
- 通常の ChatGPT: 会話・ボイスを持つ ✅ / 認証付きURLへ到達できない ❌(検索経由のため)

本仕様はこの制約を**役割分担として設計に昇格**させたもの。

## AIの実体

| 役割 | 実体 | Hubへのアクセス |
|---|---|---|
| ヨスガ | 通常の ChatGPT(ボイス・メモリあり) | **なし**(Morning Brief を貼ってもらう) |
| レコル | カスタムGPT + Actions(`server/openapi.yaml`) | 読み取り(api.php / `X-Yosuga-Token` ヘッダー) |

## 「レコルがHub更新」の実現方法(重要)

レコルはサーバーへ**直接書き込まない**。更新は既存の経路をそのまま使う:

```
レコルが回答JSON v2 を出力 → シロがアプリで取り込み → 承認 → Room反映
  → 取り込み成功時に自動でサーバーへ再同期(実装済み)
```

理由:
1. **提案→承認→保存**(v3以来の最重要原則)を崩さない。直接書込は承認を飛ばす
2. **Room(アプリ)が唯一の正**。サーバーは配信用ミラー。書込口を増やすと正が二重になる
3. サーバー側の実装追加が不要(upload.php はアプリ専用のまま)

「Hubを唯一の正とする」の Hub は**アプリ(Room)**を指し、サーバーはその写しである。
将来レコルの直接書込(専用受口PHP)を検討する場合も、承認フローを経由する形に限る。

## Morning Brief の実現方法

レコルが Actions で index → 必要ファイルを読み、Markdown で生成する。
**アプリ実装の変更は不要**(プロンプトで実現)。生成手順は `docs/recoru_prompt.md` に記載。
シロがそれをコピーしてヨスガの会話に貼る。

## 各担当の対応表

| レコルの担当(指示本文) | 現状実装での実現 |
|---|---|
| タスク整理 | 回答JSON `proposals.tasks[]` → 承認 |
| 文書分類 | `classifications[]` → 取込時に適用(v4.1 実装済み) |
| diary生成 | `diary[]` → 承認。**主語はレコルに変更**(従来はヨスガ視点) |
| projectHealth分析 | `projectHealth[]` → 承認 |
| directives更新 | `directives[]` → 承認 → directives.json 配信(v4.2 実装済み) |
| conversations整理 | conversations.json は取込・承認履歴として自動生成(現行どおり) |
| index管理 | api.php?file=index(現行どおり。管理作業は不要) |

## プロンプトの分割

- `docs/yosuga_prompt.md` — **会話の相棒用に書き換え**。JSONは書かない。
  Morning Brief の受け取りと、セッション終了時の「セッションまとめ」出力を担当
- `docs/recoru_prompt.md` — **新規**。回答JSON v2 の全書式・分類・指示書・
  Morning Brief 生成手順をこちらへ移設

## 決めたこと(このセッションの判断)

1. 観察日記の主語は**レコル**へ(セッションまとめを材料に書く)
2. アプリUIの「ヨスガ画面」「ヨスガからの提案」等の文言は**当面そのまま**
   (提案の作者は実際にはレコルになるが、改名は実機検証後にまとめて行う。積み残しへ)
3. 従来の手動ブリッジ(状況JSON)は**フォールバックとして維持**

## 将来検討

- レコル直接書込用の受口PHP(承認フロー経由を条件に)
- Morning Brief の定型化(アプリが brief.json を生成する案)— まずはプロンプト運用で様子見
- MCP(v4 Phase4)への移行時、ヨスガ/レコルを1つのAIに再統合できるかの再評価
