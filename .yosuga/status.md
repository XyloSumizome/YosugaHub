# Project Status

Yosuga Hub / 生成: 2026-07-26T11:00:00+09:00 / commit: 25004c5

## Summary

設計 v5「Obsidian ブリッジ」。Hub は AI ではなく**情報を運ぶハブ**で、知識の正本は
Obsidian の Markdown。

2026-07-26 に **実機(Pixel 10a / Android 17)を導入**し、長く未検証だった項目を
まとめて確かめた。マイグレーション5件が実機で成功し、Vault への書き込み(新規11件)と
**ノートの上書き反映(更新1件・枝番なし)が実機で確認できた**。
単体テスト 385 件が通り、Room は v8。

## Current Goal

**実機での通し運用へ移る。**
入り口(共有シート・クリップボード)は**両方とも実機で通った**。上書き反映と
Vault 書き込みも確認済み。残るのはカレンダーの実データ、Obsidian + Remotely Save、
AI 側の指示の貼り直し。

## Completed

- v5 Phase 1〜3(Obsidian 読み書き / GitHub からノート取得 / 会話ログ保存)
- UI をオペレーションコンソール化し、Material の素の部品を全廃
- `[x]` チェックの当たり判定を 48dp へ拡大
- 共有シートでレコルの回答JSONを受け取る
- `tasks.json` に `completedAt` を出す
- 近況報告に直近2週間の修正ログとカレンダー±14日を載せる
- サンプルデータを機能ごと廃止し、プロジェクト追加UIを新設
- 3ゲーム(ANRI / Kamieru / GengeKyou)を登録し、GitHub 取得まで確認
- v3 のスキーマ書き出し事故を修正し `MIGRATION_3_4` を冪等化(実バグ)
- GitHub へ初回 push(108コミット。バックアップ不在を解消)
- Hub 自身の知識ノート(`.yosuga/notes/`)を4本
- Morning Brief / 巡回 / 観測日記を実データで一周
- コンソールのコマンドを1行表記にし、ヨスガへ渡す口を1つに統合
- 状況JSON の全項目に「いつ」を持たせる
- Vault を更新反映する形にする(修正は上書き / 削除は報告 / 日記は同日差し替え)
- 使い方の HTML(`docs/how-to-use.html`)
- **実機でマイグレーションテスト5件を通す**(2026-07-26)
- **実機を運用機として立ち上げる**(プロジェクト4件 + GitHub トークン + 取得確認)
- **Vault を実機の共有ストレージへ据え、ノート11件の書き込みと振り分けを確認**
- **ノートの上書き反映を実機で確認**(更新1件・枝番なし。実ファイルで裏取り済み)
- **公開リポジトリに露出していたサーバートークンを塞ぐ**
- **共有シート経由の取り込みを実機で確認**(Hub が出て、URLではなく JSON 本文が届く)
- **クリップボード自動入力を実機で確認**(JSON として取り出せるときだけ欄に入る)
- **取り込み成功時のサーバー自動再同期を実機で確認**
- **生成したサーバートークンをコピーできるようにする**(マスク欄から取り出せなかった)

## In Progress

なし。

## Next Tasks

1. **AI 側の指示を貼り直す**(high)
   レコル(`recoru_prompt.md` のシステムプロンプト。**「ウェブを検索しない」を追加した**)/
   ヨスガ(セッションまとめの Frontmatter と観測日記の封筒)/
   ゲーム(`since` の追加・書き換え禁止の撤廃)。
   ⚠ あわせて**レコルの GPT 設定でウェブ検索機能を OFF** にする。
   ONだと「巡回」を外部サイトを見に行くことだと解釈して時間がかかる。
2. **カレンダーを実データにする**(medium)
3. **Obsidian + Remotely Save で Vault を Dropbox 同期する**(medium)
   Vault は `/sdcard/Obsidian/Yosuga`(共有ストレージ)。Hub の書き込みは確認済み。

## Blockers

なし。

## Recent Changes

- 2026-07-26 config.example.php の初期値を api.php が見ている目印に合わせる(25004c5)
- 2026-07-26 生成したサーバートークンをコピーできるようにし、レコルへウェブ検索の禁止を足す(f4c42f9)
- 2026-07-26 スキーマ事故のノートへ実機でのマイグレーション確認を追記(b988b98)
- 2026-07-26 サーバートークンを Git 管理外にし、ひな形を追加(b8ceaab)
- 2026-07-25 Vault を整理されて更新されていく形にする(86b2589)
- 2026-07-25 ヨスガへ渡す口を1つに統べ、使い方のHTMLを書く(120a770)
- 2026-07-25 状況JSON の全項目に「いつ」を持たせる(2e720a7)
- 2026-07-25 コンソールのコマンドを1行にする(2319f1e)
- 2026-07-25 回答JSON → Hub でクリップボードを自動で入れる(f71600f)
- 2026-07-25 封筒の無い回答JSONを受け取れるようにする(dc1b414)
- 2026-07-25 レコル用プロンプトを8000字上限に収める(108a617)
- 2026-07-25 v3 のスキーマ書き出し事故を直す(976c689)
- 2026-07-25 サンプルデータを廃止し、プロジェクト追加UIを作る(927c412)

全量は `git log` と `docs/WORKLOG.md` にある。

## Risks

- **AI 側の指示が古いまま動く可能性**(medium)
  貼り直さないと、封筒なしのJSONや旧コマンド名を前提に動く。
  レコルには「ウェブを検索しない」も足したので、貼り直すまで巡回が遅いままになる。
- **`connectedDebugAndroidTest` がアプリのデータを消す**(low)
  実行後にアンインストールされ、プロジェクト・トークン・Vault選択が消える。
  トークンは Keystore の鍵ごと消えるため再入力が必要。
  **実機は運用機なので、以後この端末では流さない。**
- **公開済みの履歴に古いサーバートークンが残る**(low)
  `b0c14ea1…` が origin の履歴に残る。トークンを作り直したので無効な文字列だが、
  履歴自体は消していない。ロリポップのドメインはリポジトリに書かれていないため
  到達先は分からない。

## Decisions

- 2026-07-24 **設計を v5「Obsidian ブリッジ」へ転換**
- 2026-07-25 **クリップボードの自動読み取りは採らない** — Android 12 以降は
  読むたびにトーストが出て抑制できないため。共有シートで受ける。
- 2026-07-25 **期間で絞るとき、判断できないものは捨てない** — 静かに減るより多くて正直を選ぶ。
- 2026-07-25 **サンプルデータを機能ごと廃止する**
- 2026-07-25 **プロジェクト ID は自動採番しない** — 正本なので人が決める。作成後は変更不可。
- 2026-07-25 **プロジェクトの呼称は英字名にする** — 表示名は Obsidian のフォルダ名にもなる。
- 2026-07-25 **Vault は更新反映する。ただし削除は報告のみ** — GitHubのノートは
  Claude Code だけが書くので上書きしない前提が外れた。削除は Vault が人の領分なので機械が消さない。
- 2026-07-25 **ヨスガへ渡す口は1つ**(現況は既定・過去ログは任意)
- 2026-07-25 **AIの出力揺れはアプリ側で吸収する** — 封筒の無い回答JSONも受け取る。
  ただしプロンプトは正しい形を教え続ける。
- 2026-07-26 **実機=運用機 / エミュレータ=テスト機 に分ける** —
  `connectedDebugAndroidTest` は実機に運用データを入れる前に一度だけ流し、以後回さない。
- 2026-07-26 **`server/config.php` を Git 管理外にする** — 本番トークンを平文で
  追跡しており public リポジトリで公開されていた。ひな形 `config.example.php` を追跡する。
  公開されたものは取り消せないため、修復はトークンの作り直しで行った。
- 2026-07-26 **入り口は共有シートを主・クリップボードを従とする** — 実機で両方通った。
  同じ回答JSONが2経路で同一サイズで届いた。懸念していた「共有がリンクだけ送る」は起きなかった。
- 2026-07-26 **Yosuga Hub 自身を Hub のプロジェクトとして登録する** —
  保留にしていた論点。上書き反映の検証で両側を制御できる対象が必要だったため登録した。
  `projectId` は `yosuga-hub`、表示名は `YosugaHub`(Vault の `Games/YosugaHub/` になる)。

## Questions for Yosuga

- リポジトリから消えたノートを Vault 側でも `Archive/` へ移すか。いまは報告だけ。
