# Yosuga Hub 設計方針 v3.0

**AIファースト設計版**

> 本書は v2(`yosuga_hub_android_design_v2.md`)を置き換える上位方針である。
> v2 は「状況JSON ↔ ChatGPT の橋渡しダッシュボード」を中心に据えていたが、
> v3 では **AI=頭脳 / Yosuga Hub=記憶・表示装置** という役割分担へ再定義する。
> v2 の技術資料(アーキテクチャ・パッケージ・ライブラリ候補)は引き続き有効。

---

# コンセプト

Yosuga Hubは単なるタスク管理アプリではない。

ゲーム制作に関わるあらゆる情報をAIが理解・整理し、
ユーザーの制作を支援する**「AI制作秘書」**である。

管理を目的とするのではなく、

> **「会話をすると知識が育ち、制作が前に進む」**

ことを最大の目的とする。

---

# 基本思想

## AIが考える

AI(ChatGPT)が

* 会話
* GitHub
* カレンダー
* Obsidian
* プロジェクトデータ

などから状況を理解し、

* タスク化
* 優先順位
* アイデア整理
* ナレッジ整理
* 今後の予定

を判断する。

## アプリは記録する

Yosuga Hub自身は「考える」のではなく、AIが整理した結果を

* 保存
* 表示
* 編集
* 検索

する役割に徹する。

つまり

* AI = 頭脳
* Yosuga Hub = 記憶・表示装置

という役割分担にする。

---

# Obsidianとの役割分担

## Obsidian(知識の保管庫)

保存する内容: 設計思想 / 技術メモ / 開発ノウハウ / 調査内容 / アイデア / 仕様書 / 振り返り / Markdown資料

## Yosuga Hub(制作管理)

保存する内容: タスク / 進捗 / プロジェクト状態 / 締切 / GitHub情報 / 今日やること / 完了履歴

---

# AIの役割

AIは会話内容から判断する。

例:「今日はチュートリアルを作る」と言われた場合、自動で

* 新規タスク
* 優先度
* 関連プロジェクト
* 締切候補

などを判断する。

また雑談の中から重要な情報だけを抽出し、タスク / ナレッジ / アイデア / 決定事項 へ分類する。

## Obsidianへの保存

AIは会話内容を理解し、どのノートへ保存すべきか判断する。

* ゲーム設計の話 → `GameDesign.md`
* 音楽の話 → `Music.md`
* 展示会の話 → `TGS.md`

など、適切なノートへ振り分ける。

## AIはリンクも提案する

Obsidianの強みはノート同士を繋ぐことである。
AIは 関連ノート / リンク候補 / タグ候補 も提案する。

---

# 初期実装

最初は AIが「ここへ追加すると良い」という提案を表示し、ユーザーがOKを押して保存する。
いきなり自動更新は行わない。**安全性を優先する。**

---

# Android初期版

まずはAndroidだけで完結させる。

```
Android
  ↓
Yosuga Hub
  ↓
Obsidian Vault(スマホ内)
```

クラウド同期は行わない。

## 将来構想

後から以下へ対応できる設計にする(保存場所は差し替え可能な構造):

* GitHub / NAS / Dropbox / Google Drive / Obsidian Sync

---

# GitHubとの連携

GitHubはコード管理だけでなく知識共有にも利用できるよう設計する。
将来的にはプロジェクト毎に **Knowledge Repository** を持てる構造が望ましい。

---

# Claude Codeとの連携

各ゲームプロジェクトには Claude Code が存在する。
各Claude Codeは共通フォーマットで進捗情報を出力する。

例: 現在作業中 / 完了 / 問題点 / 次にやること

これをYosuga Hubが読み込み、AIが統合して整理する。

---

# 会話中心の制作

制作中、ユーザーは「今日は〇〇を進めたい」「この仕様を変更したい」「このアイデアどう思う?」と話すだけでよい。
AIは必要に応じて タスク生成 / Obsidian整理 / GitHubとの関連付け / スケジュール更新 を提案する。

---

# 長期ビジョン

Yosuga Hubはタスク管理アプリではない。
AI・Obsidian・GitHub・制作データを結び付ける **ゲーム制作の司令塔** になることを目指す。

知識は蓄積され、AIはそれを理解し、会話するほど制作環境全体が成長していく。
最終的には「シロさんが話しかけるだけで制作が整理される」体験を実現する。

---

# 重要事項(設計の最優先原則)

設計の判断に迷った場合は、以下を最優先とする。

**「機能を増やす」のではなく、「AIとの自然な会話だけで制作が前へ進む体験」を実現すること。**

---
---

# 付録: 実装方針の確定事項(2026-07-23)

> ここから下は、v3本文を Yosuga Hub の**現状の実装**へ接続するための確定事項。
> 本文(理念)と食い違う場合は本文を優先し、本付録を更新する。

## 現状の実装(v3の土台として再利用する)

旧 Phase 0〜2 で実装済み。v3ではこれらを**捨てずに積み増す**。

- Jetpack Compose + Navigation + 5画面(ホーム / カレンダー / プロジェクト / よすが連携 / 設定)
- Room(`Project` / `CalendarEvent` / `Recommendation`)。初回シードは仮データ
- DataStore(最終同期時刻)
- UI → ViewModel → Repository → DataSource の分離(手動DI)
- 状況JSON書き出し / 回答JSON取り込み(= v3の「AI提案」「JSON Import」の土台)

## 確定した技術選択

1. **AIブリッジは当面「手動JSONブリッジ」を継続。**
   ChatGPTに状況JSONを渡す → 提案JSONを受け取りアプリに取り込む → 提案カードで承認 → Task/Obsidian へ反映。
   クラウド不要・安全優先の方針に合致。既存の export/import 機構を再利用する。
   将来のAPI直結に備え、取り込み経路は差し替え可能に保つ(今は実装しない)。

2. **Obsidian連携は SAF(Storage Access Framework)で Vault フォルダを選択。**
   ユーザーが端末内の実 Obsidian Vault フォルダを選び、`DocumentFile` 経由で `.md` を読み書きする。
   選択した Vault の権限(URI パーミッション)は永続化する。クラウド同期なし・端末内で完結。
   → 提案JSONの承認結果を、指定ノート(`GameDesign.md` 等)へ**追記**する。

3. **提案→承認→保存フローを明示する。**
   自動更新はしない。AIの提案は必ず提案カードで表示し、ユーザーがOKを押して初めて Task / Obsidian へ保存する。

## v3ロードマップ(旧Phase 3「GitHub進捗取得」は後ろへ延期)

> 番号は v3 の新しい通し番号。旧 Phase 0〜2 の完了物は「土台」として上記に集約済み。

### v3-Step 1: 管理の実体化(Task を第一級エンティティに)
現状 Project に文字列で埋め込まれている作業内容を、独立した Task として扱えるようにする。
- `TaskEntity` / DAO / Repository を追加(Room)
- タスク一覧・追加・編集・完了、プロジェクト編集の最小UI(現状は読み取り専用シードのみ)
- v3 Phase 1 の CRUD 相当。ここで初めて「編集できるアプリ」になる

### v3-Step 2: 提案→承認フローの明確化
既存の回答JSON取り込みを「提案レビュー画面」へ発展させる。
- 提案を Task / 知識(Obsidian行き) に振り分けて**承認保存**、棄却も可能に
- 回答JSONスキーマを拡張:タスク項目に加え、Obsidian振り分け情報(`targetNote` / リンク候補 / タグ候補)を持たせる
  - schemaVersion を上げ、旧バージョンとの互換を維持(未知項目は無視の既存方針を踏襲)

### v3-Step 3: Obsidian連携(SAF)
- SAF で Vault フォルダ選択 → URI パーミッション永続化(DataStore に保存)
- 承認した知識を指定ノートへ追記。リンク候補 `[[...]]` / タグ `#...` を末尾へ反映
- 保存先を抽象化(`KnowledgeStore` interface)し、将来 GitHub / Drive へ差し替え可能に

### v3-Step 4: 会話中心の統合表示(磨き込み)
- ホームを「AI秘書」視点へ再編(今日やること / 承認待ちの提案 / 最近の決定事項)

### 将来(v3 Phase 3 相当 / 未着手)
- GitHub Knowledge Repository 連携、Claude Code status の統合表示
- ストレージ抽象化の拡張(NAS / Dropbox / Google Drive / Obsidian Sync)
- AI API 直結(手動ブリッジからの移行)
